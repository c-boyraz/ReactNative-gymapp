import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  home: {
    alignItems: 'center', // Equivalent to text-align: center;
  },
  heroImage: {
    width: '100%',
    height: undefined,
    aspectRatio: 1, // Adjust as necessary to maintain aspect ratio
  },
});

export default styles;
