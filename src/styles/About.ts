import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  about: {
    alignItems: 'center', // text-align: center;
  },
  profilePicture: {
    width: 150,
    height: 150,
    borderRadius: 75, // 50% of width or height for a circle
    marginBottom: 20,
  },
  button: {
    marginTop: 20,
  },
});

export default styles;
